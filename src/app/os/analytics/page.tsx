import { getAdminUser } from "@/lib/auth/admin"; import { createClient } from "@/lib/supabase/server"; export const dynamic='force-dynamic';
export default async function AnalyticsPage(){const user=await getAdminUser();const s=await createClient();const reservation=await s.from('reservations').select('*',{count:'exact',head:true});const campaign=await s.from('os_campaigns').select('*',{count:'exact',head:true});return <section><p className="text-xs font-bold tracking-[0.2em] text-palem-green uppercase">Evidence only</p><h1 className="mt-2 font-serif text-5xl">Analytics</h1><p className="mt-4 max-w-2xl text-dark-green/65">Metrik hanya ditampilkan ketika sumber data nyata tersedia dan peran Anda memiliki akses.</p><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3"><Metric label="Reservation records" value={reservation.error?null:reservation.count}/><Metric label="Campaign records" value={campaign.error?null:campaign.count}/><Metric label="Sales and menu performance" value={null}/></div><p className="mt-8 text-xs text-dark-green/45">Signed in as {user?.role.replaceAll('_',' ')}. Campaign reach, engagement, promotion usage, and sales remain unavailable until verified sources are integrated.</p></section>}
function Metric({label,value}:{label:string;value:number|null}){return <article className="rounded-lg border border-dark-green/10 bg-white p-6"><p className="text-xs font-bold tracking-wide text-palem-green uppercase">{label}</p><p className="mt-4 font-serif text-5xl">{value??'—'}</p>{value===null?<p className="mt-2 text-xs text-dark-green/45">Data source unavailable</p>:null}</article>}
