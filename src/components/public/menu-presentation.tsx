/* Signed private Media Library URLs are intentionally rendered without optimization. */
/* eslint-disable @next/next/no-img-element */
import { Container } from "@/components/layout/container";

const rupiah = new Intl.NumberFormat("id-ID", { style: "currency", currency: "IDR", maximumFractionDigits: 0 });
export function MenuPresentation({ categories, items }: { categories: Record<string, unknown>[]; items: Record<string, unknown>[] }) {
  return <section className="py-20 sm:py-28"><Container><p className="text-xs font-bold tracking-[0.22em] text-palem-green uppercase">From the kitchen</p><h1 className="mt-4 font-serif text-6xl leading-none sm:text-8xl">The menu.</h1>{items.length?<div className="mt-14 space-y-16">{categories.map((category)=>{const categoryItems=items.filter((item)=>item.category_id===category.id);if(!categoryItems.length)return null;return <section key={String(category.id)}><h2 className="border-b border-dark-green/15 pb-4 font-serif text-4xl">{String(category.name)}</h2><div className="mt-6 grid gap-5 lg:grid-cols-2">{categoryItems.map((item)=><article key={String(item.id)} className="flex gap-5 rounded-lg border border-dark-green/10 bg-white p-5">{item.image_url?<img src={String(item.image_url)} alt={String(item.image_alt||item.name)} className="size-24 shrink-0 rounded-md object-cover"/>:null}<div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-4"><h3 className="font-serif text-2xl">{String(item.name)}</h3><p className="shrink-0 text-sm font-bold text-palem-green">{rupiah.format(Number(item.price))}</p></div>{item.description?<p className="mt-2 text-sm leading-6 text-dark-green/60">{String(item.description)}</p>:null}</div></article>)}</div></section>})}</div>:<p className="mt-14 rounded-lg border border-dashed border-dark-green/20 bg-white px-6 py-16 text-center text-dark-green/60">No menu items are available in this preview.</p>}</Container></section>;
}
